[build-system]
requires = ["setuptools", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "cpse"
version = "0.1.0"
dependencies = [
    "ortools==9.11.4210",
    "unified-planning"
]
authors = []
description = "Constraint Programming Scheduler Engine"
readme = "README.md"
license = "MIT"
license-files = ["LICENSE"]